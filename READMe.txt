Setup Information:

1. Database used is LocalDb (v11.0) from SQL Server 2008 R2
2. Database authentication used is Windows Authentication, so no need to update the config file
3. Just run the solution and it will automatically create the database
4. Just connect to (localdb\v11.0) to see the database. Database name is FhirSampleDB.

